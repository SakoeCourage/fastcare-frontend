export interface packageDTO {
    id: number,
    name: string,
    description?: string,
    amount: number,
    createdAt: string,
    updatedAt: string,
    createdBy: string,
    updatedBy: string
}